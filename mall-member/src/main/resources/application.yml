server:
  tomcat:
    uri-encoding: UTF-8
    threads:
      max: 1000
      min-spare: 30
    connection-timeout: 5000ms
  port: 9002
  servlet:
    context-path: /member

spring:
  cloud:
    nacos:
      serverAddr: 43.143.154.173:8848
  # dev|test|prod
  application:
    name: member
  profiles:
    active: dev
  # jackson
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd HH:mm:ss